
const todos = (state = [], action) => {
    if (action.type) {
        switch (action.type) {

            default: {
                return state;
            }

        }
    } else {
        return state;
    }
};

export default todos;